# .cursorrules
- rule: |
    Продолжай с этого состояния. Все файлы уже есть, код из предыдущего чата использовать как основу.
    Проект: RAG-UX "ИИ-исследователь".
    Технологии: Streamlit, ChromaDB, sentence-transformers, (опционально OpenAI GPT-4o).
    Оффлайн режим обязателен при OFFLINE_ONLY=true — не предлагай внешние вызовы.
    Файлы исследований в /02_clean_texts, изображения в /03_assets.
    Цель: искать инсайты в Markdown+YAML, показывать цитаты и превью изображений.
    При изменениях помни про Windows-пути и локальные превью.
