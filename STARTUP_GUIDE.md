# 🚀 Инструкция по запуску

## Проблема с localhost:3000

Если dev-сервер не запускается, выполните:

### 1. Убедитесь, что вы в правильной директории
```bash
cd app-ui
```

### 2. Установите зависимости
```bash
npm install
```

### 3. Запустите dev-сервер
```bash
npm run dev
```

### 4. Проверьте порт
- Dev-сервер запустится на http://localhost:3000
- Если порт 3000 занят, Next.js использует следующий доступный порт (3001, 3002 и т.д.)
- Проверьте вывод в терминале для точного URL

### 5. Типичные проблемы

#### Порт занят
```bash
# Убейте процесс на порту 3000 (Windows)
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

#### Кэш нужно очистить
```bash
# Удалите .next папку
rm -rf .next
npm run dev
```

#### Проблемы с node_modules
```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

## Структура проекта

```
app-ui/
├── app/                    # Next.js App Router
├── components/ui/          # shadcn/ui компоненты
├── lib/                    # Утилиты
├── public/                 # Статические файлы
├── package.json            # Зависимости
└── next.config.ts          # Конфигурация Next.js
```

## Команды

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Production build
npm start        # Запуск production сервера
npm run lint     # Проверка кода
```

## Если ничего не помогает

1. Проверьте версию Node.js (должна быть >= 18)
```bash
node --version
```

2. Переустановите зависимости
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

3. Проверьте логи в терминале при запуске `npm run dev`





