# 🔧 Устранение проблем с запуском

## Проблема: localhost:3000 не запускается

### Решение 1: Убедитесь, что вы в правильной директории

```bash
cd C:\Users\Dell\Documents\rag-ux-starter\app-ui
npm run dev
```

### Решение 2: Очистите кэш Next.js

```bash
cd C:\Users\Dell\Documents\rag-ux-starter\app-ui
rm -r .next
npm run dev
```

### Решение 3: Переустановите зависимости

```bash
cd C:\Users\Dell\Documents\rag-ux-starter\app-ui
rm -r node_modules
rm package-lock.json
npm install
npm run dev
```

### Решение 4: Проверьте порт 3000

```bash
# Проверьте, какой процесс использует порт 3000
netstat -ano | findstr :3000

# Убейте процесс (замените PID на реальный ID процесса)
taskkill /PID <PID> /F

# Запустите снова
npm run dev
```

### Решение 5: Попробуйте другой порт

```bash
# Запустите на порту 3001
npx next dev -p 3001
```

## Проверка конфигурации

Убедитесь, что файлы на месте:

- ✅ `app-ui/package.json` — зависимости установлены
- ✅ `app-ui/app/layout.tsx` — layout файл существует
- ✅ `app-ui/app/page.tsx` — главная страница существует
- ✅ `app-ui/app/globals.css` — стили настроены
- ✅ `app-ui/components.json` — конфигурация shadcn есть

## Типичные ошибки

### "Cannot find module '@/lib/utils'"
- Проверьте `tsconfig.json` — путь `@/*` должен указывать на `"./*"`

### "Error: Cannot find module 'tailwindcss'"
- Выполните `npm install`

### "Port 3000 is already in use"
- Убейте процесс на порту 3000 или используйте другой порт

## Быстрая проверка

```bash
# 1. Проверьте Node.js версию (должна быть >= 18)
node --version

# 2. Проверьте npm версию
npm --version

# 3. Перейдите в директорию
cd C:\Users\Dell\Documents\rag-ux-starter\app-ui

# 4. Проверьте установку
npm list --depth=0

# 5. Запустите dev-сервер
npm run dev
```

## Если ничего не помогает

Создайте новый Next.js проект с нуля:

```bash
cd C:\Users\Dell\Documents\rag-ux-starter
npx create-next-app@latest app-ui-new --typescript --tailwind --app
cd app-ui-new
npm run dev
```

Затем скопируйте наши файлы из `app-ui/` в `app-ui-new/`





