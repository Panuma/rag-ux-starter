# 📋 План миграции: Streamlit → Next.js + shadcn/ui

**Цель:** Создать новый фронт на Next.js без привязки к живому бэку, с моками API.

**Маршрут:** 7 дней, 10 шагов. MVP готов к демо через неделю.

---

## 🗓️ ЧЕК-ЛИСТ ШАГОВ (по дням)

### День 1: Настройка окружения
- [ ] 1.1 Установить зависимости: добавить `framer-motion`, `zustand`
- [ ] 1.2 Настроить shadcn: добавить 5 компонентов (card, badge, separator, skeleton, toast)
- [ ] 1.3 Создать структуру папок: `/api`, `/components`, `/lib`, `/hooks`

### День 2: API-моки (Backend на фронте)
- [ ] 2.1 Создать `/app/api/search/route.ts` с моками
- [ ] 2.2 Создать `/app/api/followup/route.ts` с моками
- [ ] 2.3 Протестировать моки через Postman/curl

### День 3: Базовая разметка
- [ ] 3.1 Реализовать Layout: шапка, сайдбар, мейн-контент
- [ ] 3.2 Добавить вкладки "По тексту" / "По макету"
- [ ] 3.3 Создать форму фильтров (итерация, сценарий, дата)

### День 4: Поиск по тексту
- [ ] 4.1 Интегрировать POST `/api/search` в компонент
- [ ] 4.2 Отобразить аналитический вывод (summary + bullets)
- [ ] 4.3 Показать топ-6 цитат в карточках

### День 5: Поиск по изображениям
- [ ] 5.1 Добавить upload-зону для PNG/JPG
- [ ] 5.2 Интегрировать POST `/api/search` с `queryType: "image"`
- [ ] 5.3 Отобразить галлерею 12 похожих макетов (3x4 сетка)

### День 6: Полировка UI
- [ ] 6.1 Добавить анимации (Framer Motion): fade-in для результатов
- [ ] 6.2 Реализовать режим переключатель (Офлайн/Онлайн) в сайдбаре
- [ ] 6.3 Добавить Toast-уведомления (Sonner)

### День 7: Экспорт и тестирование
- [ ] 7.1 Реализовать кнопку "Скачать .md" (gen `ux_insights_report.md`)
- [ ] 7.2 Добавить состояние загрузки (Skeleton)
- [ ] 7.3 Провести прогон: 10 тестовых сценариев

---

## 📂 СТРУКТУРА ПРОЕКТА

```
app-ui/
├── app/
│   ├── api/
│   │   ├── search/
│   │   │   └── route.ts          # POST: text/image search
│   │   ├── followup/
│   │   │   └── route.ts          # POST: follow-up query
│   │   └── answers/
│   │       └── [id]/
│   │           └── route.ts      # GET: answer by ID
│   ├── components/
│   │   └── researcher/
│   │       ├── Header.tsx        # Заголовок + режим
│   │       ├── Sidebar.tsx       # Фильтры + настройки
│   │       ├── SearchForm.tsx    # Форма для текстового поиска
│   │       ├── ImageSearch.tsx   # Upload + галлерея
│   │       ├── ResultsCard.tsx   # Одна цитата в карточке
│   │       ├── SummaryPanel.tsx  # Аналит. вывод + bullets
│   │       └── ExportButton.tsx  # Скачать MD
│   ├── lib/
│   │   ├── utils.ts
│   │   ├── mocks.ts              # JSON-моки для API
│   │   └── types.ts              # TypeScript типы
│   ├── hooks/
│   │   ├── useSearch.ts          # Хук для POST /api/search
│   │   └── useSettings.ts        # Zustand store (режим)
│   ├── layout.tsx                # Root layout
│   ├── page.tsx                  # Главная: вкладки + routing
│   └── globals.css
├── components.json               # shadcn config
├── package.json
└── tsconfig.json
```

---

## 🔗 API-КОНТРАКТ (минимальный)

### POST `/api/search`

**Request:**
```typescript
{
  mode: "online" | "offline",
  queryType: "text" | "image",
  query?: string,           // для text
  fileIds?: string[],       // для image
  filters?: {
    iteration?: string,
    scenario?: string,
    date?: string
  }
}
```

**Response (200):**
```typescript
{
  summary: string,          // "Итог (2-4 предложения)"
  quotes: Array<{
    id: string,
    text: string,
    metadata: {
      id: string,           // R-2023-02-KK-...
      title: string,
      iteration: string,
      date: string,
      filename: string,
      section_path: string,
      chunk_index: number,
      product: string
    },
    distance?: number
  }>,
  sources: Array<{          // дедуплицированные источники
    id: string,
    title: string,
    date: string,
    iteration: string
  }>,
  images: Array<{           // связанные изображения
    path: string,
    alt?: string,
    source_id: string
  }>
}
```

---

### POST `/api/followup`

**Request:**
```typescript
{
  threadId: string,
  query: string
}
```

**Response (200):**
```typescript
{
  threadId: string,
  answerId: string,
  summary: string,
  quotes: Array<...>,       // как выше
  sources: Array<...>,
  images: Array<...>
}
```

---

### GET `/api/answers/[id]`

**Response (200):**
```typescript
{
  id: string,
  createdAt: string,
  mode: "online" | "offline",
  summary: string,
  quotes: Array<...>,
  sources: Array<...>,
  images: Array<...>
}
```

---

## 🎭 МОКИ API (JSON)

### Пример: `/app/api/search/mocks.ts`

```typescript
export const MOCK_SEARCH_RESPONSE = {
  summary: "Беспроцентный период (БП) требует явной визуализации финальной даты и легенды прогресс-бара. Обязательный платёж должен сопровождаться микрокопией о назначении. Проценты за снятие наличных/переводы нужно акцентировать заголовком и примером в день.",
  quotes: [
    {
      id: "q1",
      text: "Несколько БП считываются, но финальные даты и правило «120 дней» без явного расчёта остаются неочевидны; нужна явная дата окончания и легенда графики.",
      metadata: {
        id: "R-2023-02-KK-DebtScreenRedesign",
        title: "Редизайн экрана задолженности по кредитной карте",
        iteration: "3",
        date: "2023-02",
        filename: "R-2023-02-KK-DebtScreenRedesign.md",
        section_path: "Основные наблюдения",
        chunk_index: 0,
        product: "Кредитная СберКарта"
      },
      distance: 0.23
    },
    {
      id: "q2",
      text: "Виджет «Обязательный платёж» помогает, но часть пользователей не понимает назначение и ожидает сумму/диапазон; требуется микротекст и ориентир по сумме.",
      metadata: {
        id: "R-2023-02-KK-DebtScreenRedesign",
        title: "Редизайн экрана задолженности по кредитной карте",
        iteration: "3",
        date: "2023-02",
        filename: "R-2023-02-KK-DebtScreenRedesign.md",
        section_path: "Основные наблюдения",
        chunk_index: 1,
        product: "Кредитная СберКарта"
      },
      distance: 0.28
    },
    {
      id: "q3",
      text: "Прогресс-бар БП и точки интерпретируются неверно; требуется легенда/подписи, разграничение таймлайна и плана выплат.",
      metadata: {
        id: "R-2023-02-KK-DebtScreenRedesign",
        title: "Редизайн экрана задолженности по кредитной карте",
        iteration: "3",
        date: "2023-02",
        filename: "R-2023-02-KK-DebtScreenRedesign.md",
        section_path: "Основные наблюдения",
        chunk_index: 2,
        product: "Кредитная СберКарта"
      },
      distance: 0.31
    }
  ],
  sources: [
    {
      id: "R-2023-02-KK-DebtScreenRedesign",
      title: "Редизайн экрана задолженности по кредитной карте",
      date: "2023-02",
      iteration: "3"
    }
  ],
  images: [
    {
      path: "../03_assets/2023_02_KK_DebtScreenRedesign/R-2023-02-KK-DebtScreenRedesign_fig02.png",
      alt: "Основные наблюдения",
      source_id: "R-2023-02-KK-DebtScreenRedesign"
    }
  ]
};
```

---

## 📦 ЗАВИСИМОСТИ

```bash
npm install framer-motion zustand axios
npm install -D @types/react @types/node
```

**Текущие (уже есть):**
- next@16.0.1
- react@19.2.0
- tailwindcss@4
- shadcn/ui компоненты (button, badge, tabs, card, separator, skeleton, sonner)

**Добавить:**
```bash
npm install framer-motion zustand axios
```

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ V1

### Функциональность
- [ ] Вкладка "По тексту" работает: поиск → результаты
- [ ] Вкладка "По макету" показывает upload → галлерея
- [ ] Фильтры (итерация, сценарий, дата) применяются
- [ ] Переключатель режима (Офлайн/Онлайн) в сайдбаре функционирует
- [ ] Кнопка "Скачать .md" генерирует файл

### UI/UX
- [ ] Карточки цитат отображают все метаданные (title, id, iteration, date, filename)
- [ ] Превью изображений показываются под каждой цитатой (max 3)
- [ ] Анимация fade-in при появлении результатов (Framer Motion)
- [ ] Toast-уведомления при ошибках/успехе
- [ ] Skeleton-стейт во время загрузки

### Технические
- [ ] Нет консольных ошибок
- [ ] TypeScript компилируется без ошибок
- [ ] Все 3 мок-эндпоинта отвечают JSON
- [ ] Windows-пути к изображениям корректно обрабатываются
- [ ] Дарк-мод работает (tailwind + next-themes)

### Тестовые сценарии
1. Ввести запрос "беспроцентный период" → получить 3 цитаты
2. Включить фильтр "итерация: 3" → отфильтровать результаты
3. Переключить режим Офлайн → визуальная индикация меняется
4. Загрузить PNG → получить 12 похожих макетов
5. Нажать "Скачать .md" → файл `ux_insights_report.md` скачивается

---

## 🎯 ПОСЛЕ V1 (не входит в план)

- Подключение к живому FastAPI/Streamlit бэку
- Реальная индексация ChromaDB на фронте
- Многопоточность (follow-up вопросы)
- Настройка персонализации (избранное, история)

---

**Составил:** ИИ-ассистент  
**Дата:** 2025-01-28  
**Время выполнения:** 7 рабочих дней  
**Параллельно:** можно начать разработку бэкенда FastAPI





